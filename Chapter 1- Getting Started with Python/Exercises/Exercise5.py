#using the input function to ge the radius of the circle from the user
#using the float function to change the data type from string to a decimal
radius = float(input("enter the radius of the cirlce: "))

#calcuate the area 
area = 3.14159 * radius**2

#printing the calculated area
print("the area of the cirlcle with radius", radius,"is",area)